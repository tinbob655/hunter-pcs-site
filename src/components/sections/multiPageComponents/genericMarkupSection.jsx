import React from 'react';
import {isMobile} from '../../../index.js';
import {Link} from'react-router-dom';

function GenericMarkupSection({ /*the values in here can be referenced in the HTML */}) {
    return (
        <React.Fragment>
        </React.Fragment>
    );
};

export default GenericMarkupSection;